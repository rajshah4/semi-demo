[project]
name = "semi-demo"
version = "0.1.0"
description = "OpenHands semiconductor/foundry IT demo scaffold"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

